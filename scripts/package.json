{
  "name": "scripts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bootstrap-assets": "tsx bootstrap-assets.ts",
    "compute-precompile-address": "tsx compute-precompile-address.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'no tests'"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "viem": "^2.37.11"
  },
  "devDependencies": {
    "tsx": "^4.20.4"
  }
}
